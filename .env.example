# Configuración de Supabase
# Copia este archivo a .env.local y agrega tus credenciales reales

# URL del proyecto Supabase (puedes encontrarlo en: Settings > API)
NEXT_PUBLIC_SUPABASE_URL=

# Anon/Public Key de Supabase (puedes encontrarlo en: Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# IMPORTANTE:
# - Las variables con prefijo NEXT_PUBLIC_ son accesibles desde el navegador
# - Nunca compartas tu service_role key (solo úsala en el backend si es necesario)
# - Agrega .env.local al .gitignore para no subir credenciales
